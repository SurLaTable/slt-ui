/*
Look inside of `webpack.config.js` at the `styleguide` variable
to edit our styleguidist config.
*/
// const config = require('./docs/webpack.config.js');
// module.exports = config;

module.exports = {
	webpackConfig: require('./docs/webpack.config.js')
};
